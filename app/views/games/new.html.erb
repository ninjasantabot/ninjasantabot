<%= form_with(model: @game, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :num_days, 'Duration' %>
  <%= f.text_field :num_days %>

  <%= f.submit %>
<% end %>